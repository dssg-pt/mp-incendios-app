import Toolbar from "@mui/material/Toolbar";
import Typography from "@mui/material/Typography";

export default function Authors() {
  return (
    <div>
      <Toolbar />
      <Typography variant="h5" sx={{ margin: "1%" }}>
        Autores
      </Typography>
      <Typography sx={{ margin: "1%" }}></Typography>
    </div>
  );
}
